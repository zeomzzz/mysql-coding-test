-- Parent가 오른쪽에 오도록 Child LEFT JOIN Parent
SELECT CHILD.ITEM_ID
    , CHILD.ITEM_NAME
FROM ITEM_INFO CHILD
    LEFT JOIN ITEM_TREE PARENT
    ON CHILD.ITEM_ID = PARENT.ITEM_ID
WHERE PARENT_ITEM_ID IS NULL
ORDER BY ITEM_ID
