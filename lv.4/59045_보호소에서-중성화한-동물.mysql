SELECT n.ANIMAL_ID
     , n.ANIMAL_TYPE
     , n.NAME
FROM ANIMAL_INS n
    LEFT JOIN ANIMAL_OUTS o ON n.ANIMAL_ID = o.ANIMAL_ID
WHERE n.SEX_UPON_INTAKE LIKE "Intact%"
    AND o.SEX_UPON_OUTCOME NOT LIKE "Intact%"
ORDER BY n.ANIMAL_ID
