WITH HEAVY AS (
    SELECT HOST_ID
    FROM PLACES
    GROUP BY HOST_ID
    HAVING COUNT(ID) >= 2
)

SELECT PLACES.ID
     , PLACES.NAME
     , PLACES.HOST_ID
FROM PLACES
    INNER JOIN HEAVY ON PLACES.HOST_ID = HEAVY.HOST_ID
ORDER BY ID
